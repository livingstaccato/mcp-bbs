banking:
  enabled: true
  min_credits_to_bank: 25000
connection:
  game_letter: B
  host: localhost
  port: 2002
session:
  max_turns_per_session: 0
  target_credits: 500000
trading:
  strategy: ai
  ai_strategy:
    feedback_enabled: true
    feedback_interval_turns: 50
    goals:
      current: maximize_credits
      phases:
        maximize_credits:
          description: Maximize credits per turn through smart trading
          instructions: Focus on high-value trade routes. Avoid unnecessary combat.
          target_credits: 500000
llm:
  provider: ollama
  ollama:
    model: gemma3
    timeout_seconds: 120
upgrades:
  enabled: false
